# Synthetic Open Question

Should the fake dashboard show archived synthetic tasks by default?

Options:

1. Hide archived tasks by default and provide a filter.
2. Show archived tasks in a separate section.
3. Show all tasks in one list.

This file exists only to verify that a web advisor can read allowlisted synthetic content through MCP.
