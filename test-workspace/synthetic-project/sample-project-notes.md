# Synthetic Project Notes

This is fake project context for restricted MCP access testing.

The imaginary project is deciding whether to build a small read-only dashboard for synthetic task metadata. It contains no real customer data, credentials, source code, or private documents.

Useful facts for a connector test:

- The project has three fake tasks.
- Each fake task has a title, status, and owner role.
- No task requires file writes, shell commands, Git operations, dependency installation, or secret access.
